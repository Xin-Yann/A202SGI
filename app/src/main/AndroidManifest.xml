<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Assignment"
        tools:targetApi="31">
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".login"></activity>
        <activity android:name=".Profile_details_a" ></activity>
        <activity android:name=".Register"></activity>
        <activity android:name=".Account"></activity>
        <activity android:name=".Profile_details_b" ></activity>
        <activity android:name=".Booking_history" ></activity>
        <activity android:name=".Select_depart_ticket"></activity>
        <activity android:name=".Select_return_ticket"></activity>
        <activity android:name=".Select_seat_a"></activity>
        <activity android:name=".Select_seat_b"></activity>
        <activity android:name=".Select_seat_c"></activity>
        <activity android:name=".Select_seat_d"></activity>
        <activity android:name=".Select_seat_e"></activity>
        <activity android:name=".passengerDetailsEnd"></activity>

    </application>

</manifest>